# Go
/bin/
/spike
*.exe
*.test
*.out

# OS
.DS_Store

# Secrets : le dépôt est public. Une clé d'API commitée ici est une clé
# publiée, et la retirer ensuite ne la dépublie pas — il faut la révoquer.
.env
.env.*
!.env.example
