# Adresse du backend, telle que vue depuis le téléphone.
# Sur appareil réel, « localhost » désigne le téléphone lui-même : il faut
# l'IP de la machine qui fait tourner l'API sur le réseau local.
VITE_API_BASE_URL=http://192.168.1.10:8080
